[package]
name = "loopboard"
version = "0.1.0"
edition = "2024"

[dependencies]
lib = { path = "../../crates/lib", features = ["server", "macros", "database", "db-sqlite", "auth", "eth"] }

[workspace]
